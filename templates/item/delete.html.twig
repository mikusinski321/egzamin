{% include 'default/_form.html.twig' with {
    page_title: 'title.sure_delete_item: %title%?'|trans({'%title%': item.title|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'item_index',
} %}
