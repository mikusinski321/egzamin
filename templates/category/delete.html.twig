{% include 'default/_form.html.twig' with {
    page_title: 'title.sure_delete_category: %name%?'|trans({'%name%': category.name|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'category_index',
} %}
