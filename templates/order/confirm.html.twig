{% include 'default/_form.html.twig' with {
    page_title: 'title.order_confirm: %id%'|trans({'%id%': order.id|default('')}),
    form: form,
    submit_label: 'action.accept'|trans,
    back_to_list_path: 'order_index',
} %}