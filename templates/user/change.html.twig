{% include 'default/_form.html.twig' with {
    page_title: 'title.change_password'|trans({'%id%': item.id|default('')}),
    form: form,
    submit_label: 'action.change_password'|trans,
    back_to_list_path: 'item_index',
} %}